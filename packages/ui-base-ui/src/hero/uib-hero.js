/**
 * Compatibility entry for older imports that used @ui-base/ui/hero.
 *
 * <uib-hero> is owned by @ui-base/hero. This file intentionally contains
 * no Hero implementation so the UI package does not drift from the canonical
 * visitor-facing Hero component.
 */
import '@ui-base/hero';
export { UibHero } from '@ui-base/hero';
